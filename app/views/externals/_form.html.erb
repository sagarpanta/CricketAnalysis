<h2>
	Upload Scorecard
</h2>

<%= form_tag upload_externals_path , :multipart=>true  do %>
  <div class="fields">
    Tournament: <br />
    <%= select_tag('tournamentkey', options_for_select(@tournaments)) %>
  </div>
    <div class="fields">
    Venue: <br />
    <%= select_tag 'venuekey', options_for_select(@venues) %>
  </div>
    <div class="fields">
    Match: <br />
    <%= select_tag 'matchkey', options_for_select(@matches) %>
  </div>
  <br />
  <div class="fields">
	<%= file_field_tag :file %>
    <%= submit_tag "Import" %>
  </div> 
<% end %>


	